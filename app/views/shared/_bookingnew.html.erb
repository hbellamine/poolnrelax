<div id="booking" class="section" style="margin-top: 40px;">
  <div class="section-center">
    <div class="container animated bounceInLeft delay-15s">
      <div class="row">
        <div class="booking-form">
          <div class="booking-bg" style="background-image: url('<%=cl_image_path @pool.photo.key,height: 300, width: 400, crop: :fill, class: "img-show" %>')">
            <div class="form-header">
              <h2 id="name"><%=@pool.name %> <br>
              <p class="header-info header-first"><strong>Nb of people: <%=@pool.nbpeople %></strong> <br></p>
              <p class="header-info"><strong>Options: <%=@pool.option %></strong> <br></p>
              <p class="header-info"><strong>Price: <%=@pool.price %> € per Day</strong><br></p>
              <p class="header-info"><strong>Location: <%=@pool.location %></strong> <br></p>
            </div>
          </div>
          <%= simple_form_for [@pool,@booking] do |f| %>
          <form>
            <div class="row">
              <div class="col-md-6 text-center">
                <div class="form-group">
                 <span class="form-label"> <%= f.input :startdate  %></span>
                </div>
              </div>
              <div class="col-md-6 text-center">
                <div class="form-group">
                  <span class="form-label"><%=f.input :enddate %></span>
                </div>
              </div>
            </div>
            <div class="row d-flex justify-content-center text-center">
              <%= month_calendar events: @bookingsconfirmed do |date,bookings| %>
                <%= date.day %>
              <% bookings.each do |booking| %>
            </div>
              <% end %>
            <% end %>
            </div>
            <% if @booking.enddate != nil %>
              <h1>Price : <%= @pool.price * (@booking.enddate - @booking.startdate).to_i %> € </h1>
            <%end%>


            <%=f.submit "Book #{@pool.name} ", class: "book-button" %>

          </form>
          <%end%>
        </div>
      </div>
    </div>
  </div>
</div>
                              <% flash.each do |type, msg| %>
                    <div class=" slideIn alert alert-secondary" role="alert">
                      <%= msg %>
                    </div>
                  <% end %>

