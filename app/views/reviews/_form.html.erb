
<%= simple_form_for([pool, review], remote: true) do |f| %>
  <%= f.input :content, label: "Content", style: "color: purple" %>
  <%= f.input :rating, collection: [1, 2, 3, 4, 5] %>
  <%= f.submit "Rate #{pool.name} 🐶", class: "book-button"%>
<% end %>
